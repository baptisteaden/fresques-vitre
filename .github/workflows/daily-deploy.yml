name: daily deploy

on:
  schedule:
    - cron: '0 21 * * *'

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy
      uses: fjogeleit/http-request-action@v1
      with:
        url: ${{ secrets.DEPLOY_HOOK_URL }}
        method: 'GET'
        ref: main